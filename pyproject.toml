[project]
name = "new-choreo-qa-gen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain[google-genai]>=0.3.25",
    "python-dotenv>=1.1.0",
    "pyyaml>=6.0.2",
]
